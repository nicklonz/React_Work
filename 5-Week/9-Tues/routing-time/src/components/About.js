import React from 'react';
/*
    props => properties passed through to the component
               props are IMMUTABLE, ie can not be changed
*/
const About = (props) => {
    return (
        <h2>About Page</h2>
    )
}

export default About;